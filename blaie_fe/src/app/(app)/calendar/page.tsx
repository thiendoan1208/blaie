import { PagePlaceholder } from "@/shared/ui/page-placeholder";

export default function CalendarPage() {
  return <PagePlaceholder title="Calendar" description="Placeholder cho calendar." links={[{ href: "/", label: "Home" }]} />;
}
