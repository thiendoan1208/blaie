import { PagePlaceholder } from "@/shared/ui/page-placeholder";

export default function InboxPage() {
  return <PagePlaceholder title="Inbox" description="Placeholder cho inbox." links={[{ href: "/", label: "Home" }]} />;
}
