import { PagePlaceholder } from "@/shared/ui/page-placeholder";

export default function NotesPage() {
  return <PagePlaceholder title="Notes" description="Placeholder cho notes." links={[{ href: "/", label: "Home" }]} />;
}
