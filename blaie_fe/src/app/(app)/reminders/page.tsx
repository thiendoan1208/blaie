import { PagePlaceholder } from "@/shared/ui/page-placeholder";

export default function RemindersPage() {
  return (
    <PagePlaceholder title="Reminders" description="Placeholder cho reminders." links={[{ href: "/", label: "Home" }]} />
  );
}
