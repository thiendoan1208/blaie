import { PagePlaceholder } from "@/shared/ui/page-placeholder";

export default function TasksPage() {
  return <PagePlaceholder title="Tasks" description="Placeholder cho tasks." links={[{ href: "/", label: "Home" }]} />;
}
