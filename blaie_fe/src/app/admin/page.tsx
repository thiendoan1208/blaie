import { PagePlaceholder } from "@/shared/ui/page-placeholder";

export default function AdminPage() {
  return <PagePlaceholder title="Admin" description="Placeholder cho admin surface." links={[{ href: "/", label: "Home" }]} />;
}
